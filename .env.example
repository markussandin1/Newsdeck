# NewsDeck Environment Variables

# ===================================
# IMPORTANT: Secrets Management
# ===================================
#
# DEVELOPMENT: Use environment variables (this file)
# PRODUCTION: Secrets are managed in GCP Secret Manager
#
# In production, the following secrets are automatically loaded from GCP Secret Manager:
# - newsdeck-database-url
# - newsdeck-nextauth-secret
# - newsdeck-google-client-id
# - newsdeck-google-client-secret
# - newsdeck-api-key
#
# See GCP_SECRET_MANAGER_SETUP.md for setup instructions
# ===================================

# ===================================
# Database Configuration
# ===================================

# PostgreSQL Database URL
# For local development with Docker:
DATABASE_URL=postgresql://newsdeck:newsdeck_local@localhost:5433/newsdeck_dev

# For production (Cloud SQL, Supabase, etc.):
# DATABASE_URL=postgresql://user:password@host:5432/dbname
# Note: In production, this is loaded from GCP Secret Manager

# ===================================
# Authentication (NextAuth)
# ===================================

# Google OAuth credentials
# Get these from: https://console.cloud.google.com/apis/credentials
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# Note: In production, these are loaded from GCP Secret Manager

# NextAuth secret (generate with: openssl rand -base64 32)
# NEXTAUTH_SECRET=your-nextauth-secret-min-32-chars
# Note: In production, this is loaded from GCP Secret Manager

# ===================================
# API Security
# ===================================

# API Key for securing endpoints (optional for local dev)
# Generate with: openssl rand -base64 32
# API_KEY=your_secret_api_key_here
# Note: In production, this is loaded from GCP Secret Manager

# ===================================
# GCP Configuration
# ===================================

# Google Cloud Project ID (required for Secret Manager in production)
GCP_PROJECT_ID=newsdeck-473620

# ===================================
# Rate Limiting
# ===================================

# Rate limiting uses PostgreSQL (DATABASE_URL above)
# No additional configuration needed!
# Current limit: 100 requests per minute per workflow
# Adjust in lib/rate-limit.ts if needed

# ===================================
# Development Settings
# ===================================

# Set to 'development' for local dev, 'production' for prod
NODE_ENV=development
